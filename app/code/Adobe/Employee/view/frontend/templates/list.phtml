<h2>My Employees</h2>

<a href="<?= $escaper->escapeUrl($block->getUrl('employee/account/add')) ?>"
   style="margin-bottom:10px; display:inline-block;">Add Employee</a>

<table border="1" cellpadding="5" cellspacing="0" style="width:100%; border-collapse:collapse;">
    <thead>
        <tr>
            <th>Name</th>
            <th>Joining Date</th>
            <th>Designation</th>
            <th>Address</th>
            <th>Status</th>
            <th>Hobbies</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>

    <?php $employees = $block->getEmployees(); ?>

    <?php if ($employees && count($employees)): ?>
        <?php foreach ($employees as $emp): ?>
            <tr>
                <td><?= $escaper->escapeHtml($emp->getName()) ?></td>
                <td><?= $escaper->escapeHtml($emp->getJoiningDate()) ?></td>
                <td><?= $escaper->escapeHtml($emp->getDesignation()) ?></td>
                <td><?= $escaper->escapeHtml($emp->getAddress()) ?></td>
                <td><?= $emp->getStatus() ? 'Active' : 'Inactive' ?></td>
                <td><?= $escaper->escapeHtml($emp->getHobbies()) ?></td>
                <td>
                    <a href="<?= $escaper->escapeUrl($block->getUrl('employee/account/edit', ['id'=>$emp->getId()])) ?>">Edit</a> |
                    <a href="<?= $escaper->escapeUrl($block->getUrl('employee/account/delete', ['id'=>$emp->getId()])) ?>"
                       onclick="return confirm('Are you sure you want to delete this employee?')">Delete</a>
                </td>
            </tr>
        <?php endforeach; ?>
    <?php else: ?>
        <tr>
            <td colspan="7">No employees found.</td>
        </tr>
    <?php endif; ?>

    </tbody>
</table>
